{
	"name": "rifolks-drifts-backend",
	"version": "1.0.0",
	"description": "Backend for Rifolks Drifts",
	"main": "server.js",
	"scripts": {
		"start": "node server.js",
		"dev": "nodemon server.js",
		"test": "jest",
		"lint": "eslint .",
		"format": "prettier --write .",
		"migrate": "node -e \"const { execSync } = require('child_process'); const maxRetries = 5; let retries = 0; const delay = ms => new Promise(resolve => setTimeout(resolve, ms)); while (retries < maxRetries) { try { console.log('Attempting database migration...'); execSync('npx prisma migrate deploy --accept-data-loss', { stdio: 'inherit' }); console.log('Migration successful!'); process.exit(0); } catch (error) { retries++; if (retries === maxRetries) { console.error('Migration failed after', maxRetries, 'attempts'); process.exit(1); } console.log('Migration attempt failed, retrying in 10 seconds...'); await delay(10000); } }\"",
		"migrate:dev": "prisma migrate dev",
		"migrate:reset": "prisma migrate reset",
		"generate": "prisma generate",
		"studio": "prisma studio",
		"build": "prisma generate && tsc",
		"postinstall": "prisma generate"
	},
	"engines": {
		"node": ">=18.0.0"
	},
	"dependencies": {
		"@prisma/client": "^5.10.2",
		"@supabase/supabase-js": "^2.39.3",
		"bcryptjs": "^2.4.3",
		"cors": "^2.8.5",
		"dotenv": "^16.4.5",
		"express": "^4.18.2",
		"express-rate-limit": "^7.1.5",
		"helmet": "^7.1.0",
		"jsonwebtoken": "^9.0.2",
		"morgan": "^1.10.0",
		"pg": "^8.11.3",
		"winston": "^3.11.0"
	},
	"devDependencies": {
		"jest": "^29.7.0",
		"nodemon": "^3.0.3",
		"prisma": "^5.10.2",
		"supertest": "^6.3.4"
	}
}
